// Trooth Social Independent — Live Share + Activity v3
(function(){
  function boot(){
    var sb=window.troothSupabase;if(!sb)return setTimeout(boot,300);
    var feed=document.getElementById('feed');if(!feed)return;
    if(window.troothShareActivityLiveReady)return;window.troothShareActivityLiveReady=true;
    var user=null,channel=null,timer=null,retryTimer=null,authSub=null,refreshing=new Set(),recentShares=new Map();
    var DEDUPE_TTL=12000;
    function postId(post){var direct=post.getAttribute('data-post-id');if(direct)return direct;return (post.querySelector('[onclick*="likePost"]')?.getAttribute('onclick')||'').match(/likePost\(['"]([^'"]+)['"]\)/)?.[1]||null}
    function prune(){var now=Date.now();recentShares.forEach(function(t,k){if(now-t>DEDUPE_TTL)recentShares.delete(k)})}
    async function refreshShares(id,post){if(!id||!post||refreshing.has(id)||!navigator.onLine)return;refreshing.add(id);try{var r=await sb.from('post_shares').select('id').eq('post_id',id);if(r.error)return;var el=post.querySelector('.trooth-share-count');if(!el){el=document.createElement('span');el.className='trooth-share-count';var bar=post.querySelector('.trooth-reaction-bar')||post.querySelector('.postactions');if(bar)bar.parentNode.insertBefore(el,bar.nextSibling)}if(el)el.textContent='↗ '+((r.data||[]).length)+' shares'}finally{refreshing.delete(id)}}
    async function share(id,post){if(!user){alert('Please login to share this post.');return}if(!navigator.onLine){alert('You are offline. Please reconnect and try again.');return}prune();var key=user.id+':'+id;if(recentShares.has(key)){await refreshShares(id,post);return}recentShares.set(key,Date.now());var r=await sb.from('post_shares').insert({post_id:id,user_id:user.id});if(r.error&&r.error.code!=='23505'){recentShares.delete(key);console.error(r.error);alert('Share failed. Please try again.');return}await refreshShares(id,post);var url=location.origin+location.pathname+'?post='+encodeURIComponent(id);try{if(navigator.share)await navigator.share({title:'Trooth Social Independent',text:'Check this post on Trooth',url:url});else if(navigator.clipboard){await navigator.clipboard.writeText(url);alert('Post link copied!')}else prompt('Copy post link:',url)}catch(e){}window.dispatchEvent(new CustomEvent('trooth-post-shared',{detail:{postId:id}}));window.dispatchEvent(new CustomEvent('trooth-navigation-refresh',{detail:{type:'share',postId:id}}))}
    function decorate(){feed.querySelectorAll('.post').forEach(function(post){var id=postId(post);if(!id)return;post.setAttribute('data-post-id',id);if(!post.querySelector('.trooth-share-live')){var actions=post.querySelector('.postactions');if(!actions)return;var b=document.createElement('button');b.className='trooth-share-live';b.type='button';b.textContent='↗ Share';b.setAttribute('aria-label','Share this post');b.onclick=function(){share(id,post)};actions.appendChild(b)}refreshShares(id,post)})}
    var style=document.createElement('style');style.textContent='@media(max-width:650px){.trooth-share-live{min-height:42px;padding:9px 10px}.trooth-share-count{font-size:12px}}.trooth-share-live{border:0;background:transparent;color:#2d6a4f;font-weight:700;cursor:pointer;padding:8px 10px;border-radius:10px}.trooth-share-live:hover{background:#e8f5ed}.trooth-share-count{display:block;padding:6px 4px;color:#718276;font-size:13px}';document.head.appendChild(style);
    function cleanup(){clearTimeout(timer);clearTimeout(retryTimer);timer=null;retryTimer=null;if(channel){try{sb.removeChannel(channel)}catch(e){}channel=null}window.troothShareActivityLiveChannel=null}
    function scheduleRetry(){clearTimeout(retryTimer);retryTimer=setTimeout(function(){retryTimer=null;subscribe()},1500)}
    function subscribe(){if(channel||!user||!navigator.onLine)return;channel=sb.channel('trooth-post-shares-live-'+user.id+'-'+Date.now()).on('postgres_changes',{event:'*',schema:'public',table:'post_shares'},function(e){var id=e.new?.post_id||e.old?.post_id;if(!id)return;feed.querySelectorAll('.post').forEach(function(post){if(postId(post)===id)refreshShares(id,post)});window.dispatchEvent(new CustomEvent('trooth-share-live',{detail:{postId:id,event:e.eventType}}))}).subscribe(function(status){if(status==='SUBSCRIBED')window.dispatchEvent(new CustomEvent('trooth-realtime-connected',{detail:{source:'share-activity'}}));else if(status==='CHANNEL_ERROR'||status==='TIMED_OUT'){window.dispatchEvent(new CustomEvent('trooth-realtime-status',{detail:{status:'RETRYING',source:'share-activity'}}));if(channel){try{sb.removeChannel(channel)}catch(e){}}channel=null;window.troothShareActivityLiveChannel=null;scheduleRetry()}});window.troothShareActivityLiveChannel=channel}
    async function syncAuth(){var r=await sb.auth.getUser();user=r.data&&r.data.user||null;if(user)subscribe();else cleanup();decorate()}
    new MutationObserver(function(){clearTimeout(timer);timer=setTimeout(decorate,80)}).observe(feed,{childList:true,subtree:true});syncAuth();
    var ar=sb.auth.onAuthStateChange(function(event,session){user=session?.user||null;if(event==='SIGNED_OUT'||!session){cleanup();return}if(event==='SIGNED_IN'||event==='TOKEN_REFRESHED'||event==='USER_UPDATED'){cleanup();subscribe();decorate()}});authSub=ar&&ar.data&&ar.data.subscription||null;
    window.addEventListener('online',function(){subscribe();decorate()});window.addEventListener('offline',cleanup);window.addEventListener('beforeunload',cleanup)
  }
  if(window.troothSupabase)boot();else window.addEventListener('trooth-supabase-ready',boot,{once:true});
})();
