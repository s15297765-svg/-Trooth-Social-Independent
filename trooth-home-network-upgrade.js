// Trooth Social Independent — Home Network Live Upgrade v7
(function(){
  function start(){
    var s=window.troothSupabase;if(!s||window.__troothHomeNetworkUpgradeV7)return;window.__troothHomeNetworkUpgradeV7=true;
    var tables=[['news_stories','newsHub'],['sports_stories','sportsHub'],['store_listings','storesHub'],['properties','propertyHub'],['film_fashion_stories','filmFashionHub']],refreshTimers={},lastEvent={},channel=null,retryTimer=null,starting=false,handlersBound=false,attempt=0,toastTimer=null,onlineHandler,offlineHandler,focusHandler,visibilityHandler,beforeUnloadHandler;
    function refreshHub(table,id){if(!navigator.onLine)return;if(typeof window.loadHub==='function')return window.loadHub(table,id)}
    function schedule(key,fn,delay){clearTimeout(refreshTimers[key]);refreshTimers[key]=setTimeout(fn,delay||700)}
    function shouldHandle(key){var now=Date.now();if(lastEvent[key]&&now-lastEvent[key]<900)return false;lastEvent[key]=now;return true}
    function toast(msg){var el=document.getElementById('troothHomeNetworkToast');if(!el){el=document.createElement('div');el.id='troothHomeNetworkToast';el.setAttribute('role','status');el.setAttribute('aria-live','polite');el.style.cssText='position:fixed;right:16px;bottom:16px;background:#2d6a4f;color:#fff;padding:11px 15px;border-radius:12px;box-shadow:0 5px 18px #0002;z-index:9999;font-weight:800;opacity:0;transition:.25s;pointer-events:none;max-width:calc(100vw - 32px)'}el.textContent=msg;el.style.opacity='1';clearTimeout(toastTimer);toastTimer=setTimeout(function(){el.style.opacity='0'},2200)}
    function status(type,detail){try{window.dispatchEvent(new CustomEvent('trooth-home-network-status',{detail:{status:type,message:detail||'',attempt:attempt}}))}catch(e){}}
    function cleanup(){clearTimeout(retryTimer);retryTimer=null;Object.keys(refreshTimers).forEach(function(k){clearTimeout(refreshTimers[k]);delete refreshTimers[k]});if(channel){try{s.removeChannel(channel)}catch(e){}channel=null}}
    function retry(){if(!navigator.onLine)return;attempt=Math.min(attempt+1,8);var delay=Math.min(30000,Math.max(1500,1500*Math.pow(1.7,attempt-1)));status('RETRYING','reconnect scheduled');window.dispatchEvent(new CustomEvent('trooth-realtime-status',{detail:{status:'RETRYING',source:'home-network',attempt:attempt}}));clearTimeout(retryTimer);retryTimer=setTimeout(function(){starting=false;subscribe()},delay)}
    function subscribe(){
      if(starting||!navigator.onLine)return;starting=true;cleanup();
      channel=s.channel('trooth-home-network-live-'+Date.now())
        .on('postgres_changes',{event:'*',schema:'public',table:'posts'},function(){if(!shouldHandle('posts'))return;schedule('posts',function(){if(typeof window.loadPosts==='function'&&navigator.onLine)window.loadPosts();toast('Live feed updated ✓');status('FEED_REFRESH','posts')},450)})
        .on('postgres_changes',{event:'*',schema:'public',table:'stories_reels'},function(){if(!shouldHandle('stories'))return;schedule('stories',function(){if(typeof window.loadStories==='function'&&navigator.onLine)window.loadStories();status('STORIES_REFRESH','stories_reels')},600)})
        .on('postgres_changes',{event:'*',schema:'public',table:'news_stories'},function(){if(shouldHandle('news_stories'))schedule('news_stories',function(){refreshHub('news_stories','newsHub');status('HUB_REFRESH','news_stories')},600)})
        .on('postgres_changes',{event:'*',schema:'public',table:'sports_stories'},function(){if(shouldHandle('sports_stories'))schedule('sports_stories',function(){refreshHub('sports_stories','sportsHub');status('HUB_REFRESH','sports_stories')},600)})
        .on('postgres_changes',{event:'*',schema:'public',table:'store_listings'},function(){if(shouldHandle('store_listings'))schedule('store_listings',function(){refreshHub('store_listings','storesHub');status('HUB_REFRESH','store_listings')},600)})
        .on('postgres_changes',{event:'*',schema:'public',table:'properties'},function(){if(shouldHandle('properties'))schedule('properties',function(){refreshHub('properties','propertyHub');status('HUB_REFRESH','properties')},600)})
        .on('postgres_changes',{event:'*',schema:'public',table:'film_fashion_stories'},function(){if(shouldHandle('film_fashion_stories'))schedule('film_fashion_stories',function(){refreshHub('film_fashion_stories','filmFashionHub');status('HUB_REFRESH','film_fashion_stories')},600)})
        .subscribe(function(subStatus){starting=false;if(subStatus==='SUBSCRIBED'){attempt=0;toast('Trooth Live Network connected ✓');status('CONNECTED','Live network connected');window.dispatchEvent(new CustomEvent('trooth-realtime-connected',{detail:{source:'home-network'}}))}else if(subStatus==='CHANNEL_ERROR'||subStatus==='TIMED_OUT'){retry()}});
    }
    var hero=document.querySelector('.hero');if(hero&&!document.getElementById('troothLiveBadge')){var b=document.createElement('span');b.id='troothLiveBadge';b.className='tag';b.textContent='● LIVE NETWORK';b.style.marginLeft='6px';var tag=hero.querySelector('.tag');if(tag&&tag.parentNode)tag.after(b);else hero.appendChild(b)}
    onlineHandler=function(){attempt=0;subscribe()};offlineHandler=function(){status('OFFLINE','Network offline');cleanup();starting=false};focusHandler=function(){if(!navigator.onLine)return;if(typeof window.loadPosts==='function')window.loadPosts();tables.forEach(function(x){schedule(x[0],function(){refreshHub(x[0],x[1])},250)});if(!channel)subscribe()};visibilityHandler=function(){if(!document.hidden&&navigator.onLine){focusHandler()}};
    if(!handlersBound){handlersBound=true;window.addEventListener('online',onlineHandler);window.addEventListener('offline',offlineHandler);window.addEventListener('focus',focusHandler);document.addEventListener('visibilitychange',visibilityHandler);beforeUnloadHandler=function(){window.removeEventListener('online',onlineHandler);window.removeEventListener('offline',offlineHandler);window.removeEventListener('focus',focusHandler);document.removeEventListener('visibilitychange',visibilityHandler);cleanup();clearTimeout(toastTimer)};window.addEventListener('beforeunload',beforeUnloadHandler,{once:true})}
    subscribe();
  }
  if(window.troothSupabase)start();else window.addEventListener('trooth-supabase-ready',start,{once:true});
})();
